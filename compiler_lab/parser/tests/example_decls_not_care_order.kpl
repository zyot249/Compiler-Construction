Program Example2; (* Factorial *)
Var n : Integer;
Var n : Integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  End;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  End;

Var n : Integer;
Var n : Integer;

TYPE T = INTEGER;

TYPE T = INTEGER;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  End;

TYPE T = INTEGER;

Begin
  For n := 1 To 7 Do
    Begin
      Call WriteLn;
      Call WriteI(F(i));
    End;
End. (* Factorial *)
