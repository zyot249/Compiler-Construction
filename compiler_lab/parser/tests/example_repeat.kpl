Program Example2; (* Factorial *)
   
Var n : Integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  
    Repeat 
      Begin 
        Call WriteI(1); 
      End
    Until n > 0;
  
  End;

Begin
  For n := 1 To 7 Do
    Begin
      Call WriteLn;
      Call WriteI(F(i));
    End;
End. (* Factorial *)