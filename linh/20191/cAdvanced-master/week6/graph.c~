#include <stdio.h>
#include <stdlib.h>
#include "graph.h"

Graph creatGraph(int sizemax){
  Graph newGraph;
  newGraph.sizemax = sizemax;
  return newGraph;
}

void addEdge(Graph* graph, int v1, int v2){
  graph->matrix[v1][v2] = 1;
}

int adgancent(Graph* graph, int v1, int v2){


}
